/**
 * The path to the directory containing the credentials files.
 */
export const CREDENTIALS_PATH = 'src/shared/utils/credentials/';

/**
 * The filename of the Gmail credentials file.
 */
export const GMAIL_CREDS = 'gmail_creds.json';

/**
 * Enum representing common message strings.
 */
export enum MESSAGE {
  BAD_REQUEST = 'Bad request',
  SUCCESS = 'Success',
}
